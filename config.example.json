{
  "_comment": "F4keH0und - Last Generation Configuration Example",
  "_comment_recycling": "Recycling preferences control how stale objects are discovered and prioritized",

  "RecyclingPreferences": {
    "_comment": "Set PreferRecycling to true to prioritize using existing objects over creating new ones",
    "PreferRecycling": true,

    "_comment": "Set RecyclingOnly to true to refuse deployment if no recyclable objects are found",
    "RecyclingOnly": false,

    "_comment": "Only recycle objects older than this many days (prevents recycling recently disabled accounts)",
    "MinimumObjectAgeDays": 180,

    "_comment": "Don't recycle objects older than this (may indicate ancient, possibly still-referenced objects)",
    "MaximumObjectAgeDays": 3650
  },

  "SafetyFilters": {
    "_comment": "Exclude these OUs from recycling - use wildcards with DC=*",
    "ExcludedOUs": [
      "OU=VIP,DC=*",
      "OU=Executives,DC=*"
    ],

    "_comment": "Regex patterns - never recycle users matching these patterns",
    "ProtectedUserPatterns": [
      "^admin",
      "^svc_prod"
    ]
  },

  "InventorySettings": {
    "_comment": "Persistent lifecycle inventory backend settings",
    "EnablePersistentInventory": true,
    "PreferredSource": "Auto",
    "InventoryDirectory": "./inventory",
    "EventLogFileName": "F4keH0und_Inventory_Events.ndjson",
    "SnapshotFileName": "F4keH0und_Inventory_Snapshot.json",
    "UpdateSnapshotOnWrite": true
  },

  "WindowsDeploymentSettings": {
    "_comment": "Windows-only artifact deployment settings used by New/Update/Disable/Enable/Remove-F4keH0undElement",
    "ArtifactRoot": "C:\\ProgramData\\F4keH0und-LG\\Elements",
    "Port": 5985,
    "UseSSL": false,
    "Authentication": "Negotiate",
    "ThrottleLimit": 10
  },

  "TelemetrySettings": {
    "_comment": "Primary telemetry profile and source hints for artifact elements",
    "DefaultTelemetryProfile": "Windows-Artifact-Baseline",
    "PrimarySources": [
      "Sysmon",
      "WindowsSecurity"
    ],
    "ConnectorPackPath": "./telemetry-connectors.windows.json"
  },

  "ElementRegistrySettings": {
    "_comment": "External element type registry. If missing, built-in defaults are used.",
    "EnableExternalRegistry": true,
    "RegistryPath": "./element-types.windows.json"
  },

  "RolloutProfiles": {
    "_comment": "Phase 5 rollout controls. Commands can use these profiles for safer defaults.",
    "DefaultProfile": "Pilot",
    "Profiles": {
      "Lab": {
        "DefaultWhatIf": true,
        "WindowsThrottleLimit": 3,
        "MaxEntraDeploymentsPerRun": 2,
        "AllowHighPrivilegeRoleAssignment": true
      },
      "Pilot": {
        "DefaultWhatIf": false,
        "WindowsThrottleLimit": 8,
        "MaxEntraDeploymentsPerRun": 5,
        "AllowHighPrivilegeRoleAssignment": false
      },
      "Production": {
        "DefaultWhatIf": false,
        "WindowsThrottleLimit": 15,
        "MaxEntraDeploymentsPerRun": 10,
        "AllowHighPrivilegeRoleAssignment": false
      }
    }
  }
}
